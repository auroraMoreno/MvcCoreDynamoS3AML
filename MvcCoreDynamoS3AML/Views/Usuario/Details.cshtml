@model MvcCoreDynamoS3AML.Models.Usuario

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Usuario</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.IdUsuario)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.IdUsuario)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Nombre)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Nombre)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Descripcion)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Descripcion)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.FechaAlta)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.FechaAlta)
        </dd>
    </dl>
    @*<form method="post" enctype="multipart/form-data">
        <div>
            <div>
                <h5 class="card-title">Añadir Imagen</h5>
                <input type="hidden" name="idUsuario" value="@Model.IdUsuario">
                <div>
                    <label>Titulo</label>
                    <input type="text" name="titulo">
                </div>
                <div class="form-group">
                    <label>Imagen</label>
                    <input type="file" name="imagen">
                </div>
                <button type="submit">Guardar</button>
            </div>
        </div>
    </form>

    @if (Model.Fotos != null && Model.Fotos.Any())
    {
        <table>
            <thead>
                <tr>
                    <th>Titulo</th>
                    <th>Imagen</th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (Foto foto in Model.Fotos)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => foto.Titulo)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => foto.Imagen)
                        </td>
                        <td>
                            <a asp-action="FileAWS"
                               asp-controller="Usuario"
                               asp-route-filename="@foto.Imagen">Visualizar</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }*@

    @if (Model.Fotos !=null)
        {
            <div>
                Imagenes:
                <h3>Nombre: @Model.Fotos.Titulo</h3>
                <h3>Imagen: @Model.Fotos.Imagen</h3>
                <a asp-action="FileAWS"
                   asp-controller="Usuario"
                   asp-route-filename="@Model.Fotos.Imagen"
                   >Visualizar</a>
            </div>
        }
</div>
<div>
    <a asp-action="Index">Back to List</a>
</div>
